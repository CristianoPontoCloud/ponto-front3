export interface Address {
	zipcode: string;
	neighborhood: string;
	street: string;
	code: string;
	city: string;
	state: string;
	complement: string;
}

export interface Coordinates {
	lat: string;
	lng: string;
}
